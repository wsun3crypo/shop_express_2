<%= form_with(model: @merchant_partnership, local: true) do |f| %>

  <div class="form-group">
    <%= f.label :sender_id %>

    <%= f.text_field :sender_id, class: "form-control" %>
  </div>

  <%= f.hidden_field :receiver_id, value: @merchant.id %>

  <%= f.submit "Create merchant partnership", class: "btn btn-block btn-outline-secondary" %>
<% end %>
