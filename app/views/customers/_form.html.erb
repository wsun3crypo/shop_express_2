<% was_validated = customer.errors.any? %>

<%= form_with(model: customer, local: true, novalidate: true, class: "mb-3") do |f| %>
  <%= f.button class: "btn btn-outline-secondary btn-block" %>
<% end %>
